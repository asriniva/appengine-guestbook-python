runtime: python38
runtime_channel: canary
service: guestbook3

# [START handlers]
handlers:
- url: /favicon\.ico
  static_files: favicon.ico
  upload: favicon\.ico

- url: /bootstrap
  static_dir: bootstrap

- url: /.*
  script: auto
# [END handlers]

